# -------------------------------------------------------
# Docker-specific environment variables
# Copy this to .env or source it before `docker compose up`
# -------------------------------------------------------

# Application
APP_PORT=8080

# Database
DB_DATABASE=bukku
DB_USERNAME=bukku
DB_PASSWORD=secret
DB_ROOT_PASSWORD=rootsecret
DB_EXTERNAL_PORT=3306
